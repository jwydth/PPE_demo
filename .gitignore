node_modules/
.next/
.env.local
.env
__pycache__/
*.pyc
.venv/
*.pack.gz
backend/storage/
backend/weights/*.pt
# TensorRT engines are hardware/driver-specific (PERF_PLAN.md Tier 2.1/6) and
# .onnx are just export intermediates — neither belongs in git.
backend/weights/*.engine
backend/weights/*.onnx
backend/.venv
backend/venv
video_debug.log
videos/*
be_log.txt
auto.key
auto.crt
# Working notes from this session's performance/debugging pass — not meant
# to be a tracked project doc.
PERF_PLAN.md
task.md
.codex
.agents
AGENTS.md